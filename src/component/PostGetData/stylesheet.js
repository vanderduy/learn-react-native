import React, { Component } from 'react';
import {
    StyleSheet
} from 'react-native'

export const sty = StyleSheet.create({
    showKQ:{
        color : 'red',
        fontSize:20
    },
    view:{
        flex:1,
        alignItems: 'center',
        justifyContent:'center',
        marginTop: 30,
    }
});